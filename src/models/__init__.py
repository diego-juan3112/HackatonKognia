"""Shared data structures and port contracts.

This layer is transversal (see AGENTS.md section 2): every other layer may
import it, because it carries no behaviour of its own -- only data shapes and
the Protocol definitions that describe what an adapter must provide.
"""
